apiVersion: v1
kind: Service
metadata:
  name: codemaster-web
  namespace: codemaster
  labels:
    app.kubernetes.io/name: codemaster
    app.kubernetes.io/component: web
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/name: codemaster
    app.kubernetes.io/component: web
