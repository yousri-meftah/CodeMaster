apiVersion: v1
kind: Service
metadata:
  name: piston
  namespace: codemaster
  labels:
    app.kubernetes.io/name: codemaster
    app.kubernetes.io/component: piston
spec:
  ports:
    - name: http
      port: 2000
      targetPort: http
  selector:
    app.kubernetes.io/name: codemaster
    app.kubernetes.io/component: piston
