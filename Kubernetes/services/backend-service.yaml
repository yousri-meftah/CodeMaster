apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: codemaster
  labels:
    app.kubernetes.io/name: codemaster
    app.kubernetes.io/component: backend
spec:
  ports:
    - name: http
      port: 8000
      targetPort: http
  selector:
    app.kubernetes.io/name: codemaster
    app.kubernetes.io/component: backend
